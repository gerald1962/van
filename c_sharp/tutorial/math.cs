using System;

/* The C# Math class has many methods that allows you to perform mathematical
 * tasks on numbers. */
namespace Calc {
	class Prg {
		static void Main(string[] args) {
			int  x;
		
			x = Math.Max(5, 10);
			Console.WriteLine("Math: max(5,10) -> {0}", x);
		}
	}
}
